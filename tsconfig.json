{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": "./src",
    "paths": {
      "@core": ["core"],
      "@store": ["store"],
      "@containers": ["containers"],
    }
  }
}